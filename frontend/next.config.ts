import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev indicator (the logo badge in the bottom-left in dev).
  devIndicators: false,
};

export default nextConfig;
