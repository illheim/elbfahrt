# Don't bring host node_modules into the build — wrong arch, wrong libc
node_modules

# Build artefacts that the Dockerfile will rebuild
.cache
.tmp
.strapi
build
dist

# Secrets + local-only files
.env
.env.*

# Editor / OS noise
.DS_Store
*.log
.git
.gitignore

# Docs (not needed for runtime)
README.md
